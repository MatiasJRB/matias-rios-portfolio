"use client";

import { useEffect } from "react";
import { analytics } from "@/lib/firebae";

const Firebase = () => {
  useEffect(() => {
    if (analytics) {
    }
  }, []);

  return null;
};

export default Firebase;
